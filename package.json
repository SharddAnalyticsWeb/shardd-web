{
  "name": "shardd-web",
  "version": "1.0.0",
  "private": true,
  "description": "shardd-web",
  "author": "rishimungia",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "develop": "gatsby develop",
    "start": "gatsby develop -H 0.0.0.0 -p 8000 -o",
    "build": "gatsby build --prefix-paths",
    "serve": "gatsby serve -H 0.0.0.0 -p 9000 -o",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "@react-icons/all-files": "^4.1.0",
    "aos": "^2.3.4",
    "gatsby": "^4.5.3",
    "gatsby-plugin-sass": "^5.5.0",
    "gh-pages": "^3.2.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "sass": "^1.48.0"
  }
}
